describe('issues credentials', () => {
  it('issuer creates a new credentials definition', () => {})
  it('issuer generates an invition with verifier', () => {})
  it('verifier accepts ivitation and establishes a connection', () => {})
  it('holder puts a request for license', () => {})
  it('--- just an example ---', () => {})
})